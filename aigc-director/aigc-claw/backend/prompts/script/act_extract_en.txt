You are a professional script analyst. Please carefully read the following [Full Script Text], and per instructions, extract the entirety of **Episode {act_number}**, reference the [Global Assets], and organize it into standard JSON format.

---

[Global Assets]
Character Library:
{asset_characters}

Setting Library:
{asset_settings}

---

[Full Script Text]
{outline}

---

[Important Rules]

1. Extract the entirety of Episode {act_number} from the [Full Script Text].
2. Provide a one-sentence summary for `episode_title`.
3. Put the full text of this episode into `episode_content`, preserving all details, dialogue, and actions.
4. Keep the `scenes` array empty `[]` as scene extraction will be handled in a later stage.
5. `episode_number` must be {act_number}.
6. Output ONLY pure JSON. Do NOT generate `characters` and `settings` arrays.

---

[Output Format]

Please strictly output in the following JSON structure (direct pure JSON, no ``` wrappers, no extra text):

{{
  "episode_number": {act_number},
  "episode_title": "A one-sentence summary of Episode {act_number}",
  "episode_content": "The full script text of this episode (containing all details, dialogue)",
  "scenes": []
}}
